#include <iostream>

using namespace std;

int main()
{
   printf("Soma de dois numeros \n\n");
   float A, B, SOMA;
   printf("Digite o 1.0 numero:");
   scanf("%f", &A);
   printf("Digite o 2.0 numero:");
   scanf("%f", &B);
   SOMA = A + B;
   printf("A SOMA = %f", SOMA);
   return 0;
}
